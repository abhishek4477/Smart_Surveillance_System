# Core dependencies
numpy>=1.21.0
opencv-python>=4.7.0
python-dotenv>=0.19.0

# Deep learning frameworks (install these first)
torch>=2.0.0
torchvision>=0.15.0

# Face recognition
face-recognition>=1.3.0
deepface>=0.0.79

# Database
supabase>=1.0.3

# UI and visualization
streamlit>=1.22.0
pandas>=1.5.0
matplotlib>=3.5.0
plotly>=5.13.0

# Computer vision
mediapipe>=0.9.0
pillow>=9.0.0
ultralytics>=8.0.0  # For YOLOv8

# Object tracking (install after torch)
supervision>=0.1.0
# bytetrack is not available on PyPI, we'll use a different tracking solution
# Remove bytetrack and use ByteTrack from ultralytics instead 